
import LocalizationSettings from './LocalizationSettings';

// Re-export the main component for backward compatibility
const LanguageSelector = LocalizationSettings;

export default LanguageSelector;
